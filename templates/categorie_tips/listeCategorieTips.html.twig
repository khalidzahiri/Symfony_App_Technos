{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} Liste des Categorie
{% endblock %}

{% block TitrePage%}
    Liste des Categorie pour les tips
{% endblock %}

{% block body %}

    {% for message in app.flashes('success') %}
        <div class="row justify-content-center mt-4">
            <div class="alert alert-success col-md-6 text-center">{{ message }}</div>
        </div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div class="row justify-content-center mt-4">
            <div class="alert alert-danger col-md-6 text-center">{{ message }}</div>
        </div>
    {% endfor %}

    <table class="table table-hover">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Resume</th>
        </tr>
        </thead>
        <tbody>
        {% for categorieTip in categorieTips %}
            <tr>
                <td>{{ categorieTip.nom }}</td>
                <td><a class="btn btn-primary" href="{{ path('modifCategorieTips',{'id':categorieTip.id})}}">Modifier</a><a class="btn btn-danger" href="{{ path('deleteCategorieTips',{'id':categorieTip.id})}}">Supprimer</a></td>

            </tr>
        {% endfor %}


        </tbody>



    </table>



{% endblock %}